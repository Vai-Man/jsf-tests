% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/s7_class.R
\name{jsf_run}
\alias{jsf_run}
\title{Run a JSF simulation and return a \code{JSFSimResult} S7 object}
\usage{
jsf_run(
  x0,
  rates,
  stoich,
  t_max,
  method = "operator-splitting",
  dt = 0.01,
  switching_threshold = rep(10L, length(x0)),
  enforce_do = rep(0L, length(x0)),
  seed = NULL,
  compartment_names = NULL
)
}
\arguments{
\item{x0}{Initial compartment values (integer vector).}

\item{rates}{Rate function \verb{function(x, t)}.}

\item{stoich}{Stoichiometry list (\code{nu}, \code{DoDisc}, \code{nuReactant}, \code{nuProduct}).}

\item{t_max}{Maximum simulation time.}

\item{method}{Simulation method (\code{"operator-splitting"} or \code{"exact"}).}

\item{dt}{ODE time step (default 0.01).}

\item{switching_threshold}{Integer vector of switching thresholds, one per compartment.}

\item{enforce_do}{Integer vector (0/1), lock compartment to initial regime.}

\item{seed}{Optional RNG seed (passed to Python \code{random.seed()}).}

\item{compartment_names}{Optional character vector of compartment labels.}
}
\value{
A \link{JSFSimResult} S7 object.
}
\description{
A convenience wrapper around \code{\link[=simulate_jsf]{simulate_jsf()}} that packages the output
into a typed \link{JSFSimResult} object with print and plot methods.
}
\examples{
\dontrun{
rates  <- function(x, t) c(1.0 * x[[1]], 0.5 * x[[1]])
stoich <- list(
  nu         = list(c(1L), c(-1L)),
  DoDisc     = c(1L),
  nuReactant = list(c(1L), c(1L)),
  nuProduct  = list(c(2L), c(0L))
)
result <- jsf_run(
  x0 = 10L, rates = rates, stoich = stoich,
  t_max = 5.0, compartment_names = "Population"
)
print(result)
plot(result)
}
}
